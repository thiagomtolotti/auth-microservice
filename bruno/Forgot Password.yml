info:
  name: Forgot Password
  type: http
  seq: 9

http:
  method: POST
  url: "{{api_url}}/forgot-password"
  body:
    type: json
    data: |-
      {
        "email": "thiagotolotti@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success
    request:
      url: "{{api_url}}/users/forgot_password"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "thiagotolotti@gmail.com"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: date
          value: Mon, 11 May 2026 00:58:30 GMT
        - name: server
          value: uvicorn
        - name: content-length
          value: "56"
        - name: content-type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": "Password reset token created successfully!"
          }
