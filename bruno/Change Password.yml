info:
  name: Change Password
  type: http
  seq: 7

http:
  method: POST
  url: "{{api_url}}/users/change_password"
  body:
    type: json
    data: |-
      {
        "new_password": "@aa123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
