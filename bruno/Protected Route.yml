info:
  name: Protected Route
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_url}}/protected"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
